'use strict';

import React from 'react/addons';

const LoadingSpinner = React.createClass({
  render () {

    return <span className="throbber-loader">spinning</span>;
  }
});

export default LoadingSpinner;
